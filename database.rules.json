{
  "rules": {
    "users": {
      "$uid": {
        "transactions": {
          ".read": "$uid === auth.uid",
          ".write": "$uid === auth.uid"
        }, 
        "roommates": {
          ".read": "auth != null", 
          ".write": "auth != null"
        }
      }
    }
  }
}
